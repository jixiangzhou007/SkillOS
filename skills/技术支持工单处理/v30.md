---
name: 技术支持工单处理
created_at: '2026-06-15T07:40:44Z'
updated_at: '2026-06-15T09:26:56Z'
draft: true
version: 30
---

# 技能名称：技术支持工单处理

## 触发条件
- 当客户通过邮件或 Zendesk 工单系统提交问题时触发。

## 处理流程

### 第一步：工单接收与初步分类
- **输入**：客户提交的原始工单（标题、描述、附件）。
- **动作**：
    1. 解析工单标题和描述，提取关键问题关键词（例如：“无法登录”、“报错”、“License过期”）。
    2. 根据关键词进行初步分类，分类包括：
        - `category_account`：权限/账号问题（如：密码错误、账号锁定）。
        - `category_bug`：系统Bug或配置错误（如：功能异常、版本不匹配）。
        - `category_pending`：信息不足，需要客户提供更多日志或截图。
        - `type_incident`：故障类，系统无法正常使用或出现错误（如：无法登录、页面报错、服务中断）。
        - `type_inquiry`：咨询类，客户需要操作指导或了解产品信息（如：如何导出报表、功能说明）。
        - `type_feature`：功能请求类，客户希望增加新功能或优化现有功能（如：希望增加批量导入功能）。
- **输出**：初步分类标签（包含原有的 `category_*` 和新识别的 `type_*`）及关联的风险等级（[待补充]）。

### 第二步：问题诊断与排查
- **条件**：基于初步分类结果，执行不同的诊断路径。
- **动作**：
    1. 如果是 `category_pending`，自动生成回复模板，向客户索取指定的日志文件或系统截图。
    2. 如果是 `category_account`，检查客户账号状态（如：是否过期、是否被锁定）。
    3. 如果是 `category_bug`，比对客户系统版本与已知Bug列表或配置规范。
    4. 如果是 `type_incident`，优先检查系统服务状态、网络连通性及客户本地环境（如：浏览器缓存、插件冲突）。
    5. 如果是 `type_inquiry`，直接从知识库搜索匹配的操作指南或常见问题解答（FAQ）。
    6. **如果是 `type_feature`，将请求记录至功能需求池，并通知产品经理审核。**【补充被截断的内容】
    7. **针对 `type_incident` 子流程（新增）**：
       - 首先检索 Confluence 知识库，匹配已知故障的解决方案。
       - 若 30 分钟内未找到有效方案或无法解决，自动将工单升级至二线工程师团队。
       - 升级时附带初步排查日志、已尝试的解决步骤及客户上下文。
- **输出**：诊断结果摘要、是否升级标记（`escalated: true/false`）、升级原因（如有）。

### 第三步：解决方案与回复（[待补充]）
- **动作**：[待补充] 根据诊断结果生成回复，或通知客户升级情况。

### 第四步：工单关闭与归档（[待补充]）
- **动作**：[待补充] 确认客户问题解决后关闭工单，并更新知识库（如有新方案）。

## 参数定义（[待补充]）
- 输入参数：[待补充]
- 输出参数：[待补充]
- 内部变量：[待补充]
