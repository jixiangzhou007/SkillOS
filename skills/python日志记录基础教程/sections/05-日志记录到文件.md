# 日志记录到文件

演示如何通过 basicConfig() 配置日志输出到文件，包括设置编码、日志级别，并说明显式创建 Logger 对象是更推荐的做法。

## 要点
- 使用 basicConfig(filename=..., encoding=..., level=...) 配置文件输出
- 设置 level=logging.DEBUG 可捕获所有级别的消息
- 在大型程序中应显式创建 Logger 对象而非依赖根日志记录器
