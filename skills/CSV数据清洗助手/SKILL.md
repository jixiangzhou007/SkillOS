---
name: CSV数据清洗助手
created_at: '2026-06-16T15:48:55Z'
updated_at: '2026-06-18T00:10:29Z'
description: 解决运营同学从销售系统导出的CSV文件中常见的重复行、空邮箱、金额格式错误、日期格式不统一等问题，输出干净可用的数据表和异常记录表。 用户上传或提及需要处理CSV文件，且目标是“清洗”、“去重”、“整理”或“格式化”数据。
portable_slug: csv
draft: false
domain: natural-science
domain_label: 自然科学
philosophical_dna: scientific-method
philosophical_dna_label: 科学方法
philosophical_dna_secondary:
- reductionist
- pdca
methodology: scientific
methodology_label: 科学方法
dna_lineage:
  philosophical:
  - id: scientific-method
    weight: 0.55
  - id: reductionist
    weight: 0.27
  - id: pdca
    weight: 0.18
  domain:
  - id: data-csv-clean
    version: 1.1.0
    weight: 1.0
    primary: true
  detected_at: '2026-06-18T00:10:29Z'
  domain_key: natural-science
domain_template: data-csv-clean
bench_categories:
- data-processing
- documentation
domain_template_id: data-csv-clean
epistemic:
  source: p1-csv-heritage
  source_type: conversation
  total_claims: 40
  verified: 35
  pending: 5
  preferences: 0
  errors: 0
  claim_ids:
  - ec_1781741430_e3653c
  - ec_1781741431_7964ce
  - ec_1781741432_6e5a9a
  - ec_1781741432_71941a
  - ec_1781741433_3e1795
  - ec_1781741434_8db2ad
  - ec_1781741435_ee3bda
  - ec_1781741435_5c4eb6
  - ec_1781741436_b793d8
  - ec_1781741436_3e9adf
  - ec_1781741436_2a5d63
  - ec_1781741437_ed16dd
  - ec_1781741438_49b042
  - ec_1781741439_116fba
  - ec_1781741440_e2cda5
  - ec_1781741441_4d832b
  - ec_1781741441_4889b9
  - ec_1781741442_de9fb5
  - ec_1781741443_ad9ef6
  - ec_1781741444_0248a1
  - ec_1781741444_5de04d
  - ec_1781741445_195d0e
  - ec_1781741446_a90fc1
  - ec_1781741447_89c5ed
  - ec_1781741448_4d9af3
  - ec_1781741448_673691
  - ec_1781741449_f27802
  - ec_1781741450_cb48e5
  - ec_1781741451_4f90fb
  - ec_1781741452_1ad22c
  - ec_1781741452_09f880
  - ec_1781741453_4d311a
  - ec_1781741454_72a31c
  - ec_1781741455_1e715d
  - ec_1781741456_95e724
  - ec_1781741457_fa7a9b
  - ec_1781741457_ff5f98
  - ec_1781741458_6f7874
  - ec_1781741459_fc8cd1
  - ec_1781741460_4bef55
  pending_ids:
  - ec_1781741432_71941a
  - ec_1781741435_5c4eb6
  - ec_1781741436_3e9adf
  - ec_1781741436_2a5d63
  - ec_1781741448_673691
  processed_at: 1781741460.5944989
version: 3
bench_quality:
  checked_at: 1781758117
  dna_compliance:
    score: 6/6
    passed: 6
    total: 6
    all_passed: true
  save_gate:
    smoke_pass: true
    min_with_score: 100
    tasks:
    - data-processing-036
    - data-processing-037
---

# CSV数据清洗助手

## 核心问题
解决运营同学从销售系统导出的CSV文件中常见的重复行、空邮箱、金额格式错误、日期格式不统一等问题，输出干净可用的数据表和异常记录表。

## S_trigger
- keywords: CSV清洗, 数据清洗, 去重, 重复行, 空邮箱, 金额格式, 日期格式, 清洗报告, 销售导出, 运营数据, 异常表导出, 整理数据
- context: 用户上传或提及需要处理CSV文件，且目标是“清洗”、“去重”、“整理”或“格式化”数据。
- excludes: 用户仅要求“查看”、“分析”、“统计”或“可视化”数据，未提及清洗需求时，不触发此技能。

## S_params
| 参数 | 类型 | 默认 | 说明 |
|------|------|------|------|
| file_path | string | 必填 | CSV 文件路径 |
| primary_key | string | id | 去重主键列 |
| email_column | string | email | 邮箱列名 |
| encoding | string | utf-8 | 优先 UTF-8，失败尝试 GBK |
| output_date_format | string | YYYY-MM-DD | 统一日期格式 |

## S_body
Follow these steps in order. Ask the user if anything is marked [待确认].

1.  **读取与探测**：读取用户上传的CSV文件，自动探测文件编码（优先UTF-8，失败则尝试GBK）和分隔符（逗号、制表符等）。加载前5行作为样本，识别列名和数据类型（文本、数字、日期、邮箱等）。如果文件编码无法识别或出现乱码，提示用户重新导出为UTF-8格式。
2.  **按主键去重并处理邮箱异常**：
    -   以 `id` 列作为主键进行去重。如果存在多行具有相同 `id`，则保留创建时间（或行号）最早的一行，删除其余重复行，并记录被删除的重复行数量。
    -   **if** 文件中不存在 `id` 列，则 **then** 中止流程，并提示用户“未找到主键列 `id`，请确认文件包含该列”。
    -   扫描 `email` 列，识别以下情况为“异常行”：值为空（NULL或空字符串）、格式不合法（如不含 `@` 符号、包含空格、域名部分不标准等）。将所有异常行（包含原始所有列）提取出来，并新增一列 `error_reason`，注明具体错误原因（如“空值”、“缺少@”）。将这些异常行导出为 `error_<原文件名>.csv`。**不静默删除**：在清洗后的主表中，将异常行的 `email` 列内容替换为“详见异常表”，保留该行的其他数据。
3.  **金额格式规范化与日期格式统一**：
    -   扫描所有金额列（通过列名如“金额”、“price”、“amount”或数据类型识别）。去除列中的货币符号（如 `￥`、`$`、`€`）和千位分隔符（`,`），将清理后的值转换为 `decimal` 类型。**负数标红**：如果转换后的值为负数，在清洗报告中标注该值，并提示用户。**if** 用户确认需要输出带样式的CSV，则 **then** 在生成的CSV中将负数单元格背景标红；**else** 仅在报告中列出负数所在行号和值。**格式异常处理**：如果某单元格包含无法解析为数字的文本（如“约100元”），则将该行标记为“格式异常”，并移入异常表（`error_<原文件名>.csv`），在清洗后的主表中该单元格内容替换为“格式异常”。
    -   扫描所有日期列（通过列名如“日期”、“date”、“time”或数据类型识别）。尝试将各种常见日期格式（如 `2023/01/01`、`01-01-2023`、`2023年1月1日`）统一转换为 `YYYY-MM-DD` 格式。在清洗后的文件中，为每个被转换的日期列旁边新增一列 `原值_<列名>`，记录转换前的原始值，供用户核对。**无法解析的日期**：如果某单元格无法被任何已知格式解析，则将该行标记为“日期格式异常”，移入异常表，在清洗后的主表中该单元格内容替换为“日期异常”。
4.  **生成清洗报告与输出文件**：
    -   生成一份清洗报告，包含以下指标：清洗前总行数、清洗后总行数、去重数量、异常行数（邮箱+金额+日期异常的总和）、各关键字段（如 `email`、`金额`、`日期`）的填充率（非空值行数 / 总行数 * 100%）、负数金额列表（如有）。
    -   生成清洗后的主数据文件，命名为 `cleaned_<原文件名>.csv`。
    -   将清洗报告以文本形式展示给用户。

## S_route
```markdown

## S_route
| 用户意图/条件 | 执行动作 | 备注 |
|------------|---------|------|
| 用户上传CSV并说“清洗数据” | 执行完整清洗流程（步骤1-4） | 默认流程 |
| 文件中缺少 `id` 列 | 中止流程，提示用户“未找到主键列 `id`” | 无法自动处理 |
| 文件编码无法识别 | 提示用户“编码错误，请重新导出为UTF-8格式” | 需要用户介入 |
| 用户要求“只看重复行” | 仅执行步骤2中的去重部分，并输出重复行列表 | 非完整清洗 |
| 用户要求“只检查邮箱” | 仅执行步骤2中的邮箱检测部分，并输出异常表 | 非完整清洗 |
| 金额列中出现无法解析的文本（如“约100元”） | 将该行标记为“格式异常”，移入异常表，主表中该单元格替换为“格式异常” | 极端边界场景 |
| 日期列中出现无法被任何已知格式解析的值 | 将该行标记为“日期格式异常”，移入异常表，主表中该单元格替换为“日期异常” | 极端边界场景 |
| 金额转换后为负数，且用户确认需要输出带样式的CSV | 在生成的CSV中将负数单元格背景标红；同时在报告中列出负数所在行号和值 | 用户确认分支 |
| 金额转换后为负数，但用户未确认或拒绝输出带样式CSV | 仅在报告中列出负数所在行号和值，CSV中不做样式处理 | 用户确认分支 |
| 用户上传的CSV文件为空（0行数据） | 提示用户“文件为空，请检查后重新上传”，中止流程 | 极端边界场景 |
| 用户上传的CSV文件仅包含表头（1行数据） | 执行步骤1后，提示用户“文件仅包含表头，无数据行”，中止流程 | 极端边界场景 |
| 用户上传的CSV文件包含超过100万行数据 | 执行步骤1后，提示用户“文件行数过多（>100万），建议分批处理或使用专业工具”，中止流程 | 极端边界场景 |
| 用户上传的CSV文件所有列均为空 | 执行步骤1后，提示用户“所有列均为空，无法执行清洗”，中止流程 | 极端边界场景 |
| 用户上传的CSV文件包含特殊分隔符（如竖线`|`、分号`;`） | 在步骤1中自动探测并适配分隔符，若探测失败则提示用户指定分隔符 | 边界场景处理 |
| 用户上传的CSV文件包含BOM头 | 在步骤1中自动检测并去除BOM头，确保列名解析正确 | 边界场景处理 |
```

## 清洗应答速查（单条回复、可执行）
收到 CSV/表格清洗请求时，在同一条回复给出完整可执行方案（含去重、空值、金额、日期、报告），不要只追问。

**硬性规则**
- 必须说明：主键去重策略、异常行如何处理（单独导出，不静默删除）、输出物（清洗表 + 异常表 + 报告）。
- 金额列：去掉货币符号和千分位逗号，转为 decimal；负数单独标出。
- 日期列：统一为 YYYY-MM-DD；无法解析的进异常表。

**标准步骤模板**
1. 按主键 id **去重**（duplicate/dedup），保留最早一条。
2. email 为空或格式非法 → 导出**异常表**，不静默删除。
3. 金额/日期规范化后输出**清洗报告**（总行数、去重数、异常数、字段填充率）。

## 认识论状态
> 经验 ≠ 知识。硬规则区应优先采用「已验证」声明。

### 已验证
- ✅ keywords: CSV清洗, 数据清洗, 去重, 重复行, 空邮箱, 金额格式, 日期格式, 清洗报告, 销售导出, 运营数据, 异常表导出, 整理数据
- ✅ context: 用户上传或提及需要处理CSV文件，且目标是“清洗”、“去重”、“整理”或“格式化”数据。
- ✅ excludes: 用户仅要求“查看”、“分析”、“统计”或“可视化”数据，未提及清洗需求时，不触发此技能。
- ✅ 按主键去重并处理邮箱异常：
- ✅ 以 id 列作为主键进行去重。如果存在多行具有相同 id，则保留创建时间（或行号）最早的一行，删除其余重复行，并记录被删除的重复行数量。
- ✅ if 文件中不存在 id 列，则 then 中止流程，并提示用户“未找到主键列 id，请确认文件包含该列”。
- ✅ 金额格式规范化与日期格式统一：
- ✅ 生成清洗报告与输出文件：
- ✅ 生成一份清洗报告，包含以下指标：清洗前总行数、清洗后总行数、去重数量、异常行数（邮箱+金额+日期异常的总和）、各关键字段（如 email、金额、日期）的填充率（非空值行数 / 总行数 * 100%）、负数金额列表（如有）。
- ✅ 生成清洗后的主数据文件，命名为 cleaned_<原文件名>.csv。
- ✅ 将清洗报告以文本形式展示给用户。
- ✅ 用户意图/条件 → 执行动作
- ✅ 用户上传CSV并说“清洗数据” → 执行完整清洗流程（步骤1-4）
- ✅ 文件中缺少 `id` 列 → 中止流程，提示用户“未找到主键列 `id`”
- ✅ 文件编码无法识别 → 提示用户“编码错误，请重新导出为UTF-8格式”

### 待确认
- 📋 [evidence] 读取与探测：读取用户上传的CSV文件，自动探测文件编码（优先UTF-8，失败则尝试GBK）和分隔符（逗号、制表符等）。加载前5行作为样本，识别列名和数据类型（文本、数字、日期、邮箱等）。如果文件编码无法识别或出现乱码，提示用户重新导出为UT (`ec_1781741432_71941a`)
- 📋 [evidence] 扫描 email 列，识别以下情况为“异常行”：值为空（NULL或空字符串）、格式不合法（如不含 @ 符号、包含空格、域名部分不标准等）。将所有异常行（包含原始所有列）提取出来，并新增一列 error_reason，注明具体错误原因（如“空 (`ec_1781741435_5c4eb6`)
- 📋 [evidence] 扫描所有金额列（通过列名如“金额”、“price”、“amount”或数据类型识别）。去除列中的货币符号（如 ￥、$、€）和千位分隔符（,），将清理后的值转换为 decimal 类型。负数标红：如果转换后的值为负数，在清洗报告中标注该值，并 (`ec_1781741436_3e9adf`)
- 📋 [evidence] 扫描所有日期列（通过列名如“日期”、“date”、“time”或数据类型识别）。尝试将各种常见日期格式（如 2023/01/01、01-01-2023、2023年1月1日）统一转换为 YYYY-MM-DD 格式。在清洗后的文件中，为每个被转 (`ec_1781741436_2a5d63`)
- 📋 [evidence] 用户上传的CSV文件为空（0行数据） → 提示用户“文件为空，请检查后重新上传”，中止流程 (`ec_1781741448_673691`)
