---
name: 订单提交触发-数据提取与测试流
created_at: '2026-06-15T09:42:58Z'
updated_at: '2026-06-15T09:42:58Z'
draft: true
version: 1
---

# 技能名称：订单提交触发-数据提取与测试流

## 触发条件
- 事件：用户提交订单
- 输入：订单ID（字符串）、客户ID（字符串）

## 核心流程（MetaSkill 流水线）
本流程是一个四步流水线，每一步的输出作为下一步的输入。

### 步骤1: 验证订单
- **输入**: 订单ID、客户ID
- **核心操作**: 校验订单ID格式与客户ID有效性。
- **校验规则**:
    - 订单ID：必须符合格式 `ORD-YYYY-NNN`（示例），[待补充：具体格式正则，或引用用户提供的模板]。
    - 客户ID：必须为非空字符串。
- **异常处理**:
    - 若订单ID格式错误：[待补充：根据用户选择，是高风险中断还是中风险继续]。
    - 若客户ID为空：[待补充：根据用户选择，是中断、跳过还是填充默认值]。
- **输出**: 验证通过的订单ID、客户ID，以及一个“验证状态”标记（成功/失败/警告）。

### 步骤2: 检查退款政策
- **输入**: 步骤1输出的订单ID、客户ID
- **核心操作**: 根据订单ID和客户ID，查询适用的退款政策。
- **依赖**: [待补充：需要明确退款政策的数据来源，如数据库、API或规则文件]。
- **输出**: 退款政策ID、退款有效期、允许退款的最大金额比例等关键参数。

### 步骤3: 计算金额
- **输入**: 步骤2输出的退款政策参数、原始订单金额
- **核心操作**: 根据政策参数计算应退金额。
- **依赖**: [待补充：原始订单金额的获取方式]。
- **输出**: 计算出的应退金额（浮点数）。

### 步骤4: 退款
- **输入**: 步骤3输出的应退金额、订单ID、客户ID
- **核心操作**: 调用支付网关/退款API执行退款。
- **依赖**: [待补充：退款API的接口文档或调用方式]。
- **输出**: 退款结果（成功/失败）、退款流水号。

## 风险等级定义
- **高风险**：流程必须中断，需人工介入。
- **中风险**：流程可继续，但需记录异常日志，后续人工审核。
- **低风险**：自动处理（如填充默认值），仅做记录。